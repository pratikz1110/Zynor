# Contributing to Zynor

## Branching
- Use `main` as the default protected branch.
- Create feature branches from `main`: `feature/<short-name>` or `chore/<short-name>`.

## Pull Requests
- Keep PRs focused and small.
- Include a clear description and checklist.
- Ensure CI passes before requesting review.

